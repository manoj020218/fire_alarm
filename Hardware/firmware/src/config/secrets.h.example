// ============================================================
// FireGuard — secrets.h TEMPLATE
// Copy to secrets.h, fill in real values, NEVER commit secrets.h
// secrets.h is in .gitignore
// ============================================================
#pragma once

// MQTT credentials for the broker on mqtt.iotsoft.in / dev VPS
#define MQTT_USERNAME   "your_mqtt_username"
#define MQTT_PASSWORD   "your_mqtt_password"

// Gateway device token for VPS API authentication
// Provisioned per-unit; used in X-Gateway-Token header
#define API_DEVICE_TOKEN "your_device_token_here"

// Optional: WiFi STA credentials for bench testing
#define WIFI_SSID       "your_wifi_ssid"
#define WIFI_PASS       "your_wifi_password"
